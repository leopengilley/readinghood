build:
  steps:
    - name: Install libffi
      command: apt-get update && apt-get install -y libffi-dev

    - name: Install Python Dependencies
      command: railway run pip install -r requirements.txt
